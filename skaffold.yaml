apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: vanessavmk/auth
      context: auth
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .
    - image: vanessavmk/client
      context: client
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: '**/*.js'
            dest: .
    - image: vanessavmk/tickets
      context: tickets
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .
    - image: vanessavmk/orders
      context: orders
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .
    - image: vanessavmk/expiration
      context: expiration
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .